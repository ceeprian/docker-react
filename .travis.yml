sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t ceeprian/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ceeprian/docker-react npm run test -- --coverage
